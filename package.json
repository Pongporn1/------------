{
  "name": "mint-congrats-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && git add . && git commit -m \"Deploy updates\" && git push"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  }
}
